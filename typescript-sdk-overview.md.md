https://uithub.com/parallel-web/parallel-sdk-typescript/tree/main/src

Can you create me a single .d.ts file that summarizes the entire footprint of the API so an LLM can see how the API works? Include doc-comments if available, copying them over 1:1 without changing.
