https://uithub.com/parallel-web/parallel-sdk-python/tree/main/src/parallel?maxTokens=10000000

Can you create me a single .pyi file that summarizes the entire footprint of the API so an LLM can see how the API works? Include doc-comments if available, copying them over 1:1 without changing.
